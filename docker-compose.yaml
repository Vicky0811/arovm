version: '3.8'
services:
  arovm:
    build: .
    container_name: arovm-web
    ports:
      - "80:8080"
    volumes:
      # Allows the container to manage the host's Docker engine
      - /var/run/docker.sock:/var/run/docker.sock
    restart: on-failure
